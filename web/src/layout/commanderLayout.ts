import type { CSSProperties } from 'react'

/** Единая обёртка контента под шапкой руководителя */
export const commanderPageBody: CSSProperties = {
  maxWidth: 1120,
  margin: '0 auto',
  padding: '24px 20px 48px',
}
